volumes:
  hatsuportal_data:

services:
  db:
    image: postgres:17.5
    restart: always
    environment:
      POSTGRES_USER: development
      POSTGRES_PASSWORD: development123
      POSTGRES_DB: hatsuportal
    volumes:
      - hatsuportal_data:/var/lib/postgresql/data
      - ../backend/migrations:/migrations:ro
      - ../backend/seeds:/seeds:ro
      - ./test-data-seed.sql:/test-data-seed.sql:ro
      - ./init-dev.sh:/docker-entrypoint-initdb.d/init-dev.sh
    ports:
      - '5432:5432'
